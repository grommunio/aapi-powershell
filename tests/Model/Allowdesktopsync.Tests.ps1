#
# grommunio Admin API
# grommunio administration REST API
# Version: 1.9.2
#

Describe -tag 'GrommunioAdmin' -name 'GroAdminAllowdesktopsync' {
    Context 'GroAdminAllowdesktopsync' {
        It 'Initialize-GroAdminAllowdesktopsync' {
            # a simple test to create an object
            #$NewObject = Initialize-GroAdminAllowdesktopsync
            #$NewObject | Should -BeOfType Allowdesktopsync
            #$NewObject.property | Should -Be 0
        }
    }
}
